<nav class="navbar navbar-expand-lg navbar-light bg-success">
  <div class="container">
    <%= link_to "FinanceTRACKER", root_path, class:"navbar-brand"%>
    <% if current_user %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item <%= 'active' if request.path == my_portofolio_path %>">
            <%= link_to "My Portfolio", my_portofolio_path, class: 'nav-link'%>
          </li>
          <li class="nav-item">
            <%= link_to "Friends", my_friends_path, class: 'nav-link'%>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Profile
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to edit_user_registration_path, class: 'dropdown-item' do %>
                <%= fa_icon 'edit' %> Edit Profile
              <% end %>
            </ul>
          </li>
        </ul>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item user-name">
            <%= fa_icon 'user'%><%= current_user.full_name%>
          </li>
          <li class="nav-item">
            <%= link_to("Logout", destroy_user_session_path, class: "btn btn-xs btn-danger",method: :delete) %>
          </li>
        </ul>
      </div>
    <% end %>
  </div>
</nav>